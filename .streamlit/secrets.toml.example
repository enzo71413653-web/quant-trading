# 复制这个文件为同目录下的 secrets.toml（不带 .example 后缀），
# 填入你自己的真实密钥。secrets.toml 已在 .gitignore 里，不会被提交。
#
# Streamlit云端部署：把同样的两行内容粘到
# share.streamlit.io -> 你的App -> Settings -> Secrets

ALPACA_API_KEY_ID = "你的Key ID"
ALPACA_SECRET_KEY = "你的Secret Key"
